# Copy this file to .env.local for local-only testing.
# .env.local is not committed; no requests will go to deployed Supabase/Firebase.
# Use: npm run dev (with this file as .env.local) or npm run dev:local

# Enable local testing (required)
VITE_LOCAL_TESTING=true

# Local Supabase (run: supabase start — then copy URL and anon key from the CLI output)
VITE_SUPABASE_LOCAL_URL=http://127.0.0.1:54321
VITE_SUPABASE_LOCAL_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Optional: Firebase emulators (run: firebase emulators:start --only auth,firestore)
# VITE_FIREBASE_AUTH_EMULATOR=http://127.0.0.1:9099
# VITE_FIREBASE_FIRESTORE_EMULATOR=127.0.0.1
# VITE_FIREBASE_FIRESTORE_PORT=8080

# Optional: leave empty to avoid calling production APIs during local testing
# GEMINI_API_KEY=
# VITE_FINNHUB_KEY=
